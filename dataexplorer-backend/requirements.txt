# Feeds the Docker image (see Dockerfile). Kept in lockstep with the pins in
# pyproject.toml — the image installs from here, not from the workspace uv.lock,
# so a divergence means the container runs different versions than the suite.
# tests/core/test_dependency_pins.py asserts the two agree.
#
# Shorter than curator-backend's on purpose: no pyshacl (validation gates
# writes), no python-multipart (nothing is uploaded here), no pypdf (page counts
# are derived once, on write).
fastapi==0.141.1
uvicorn[standard]==0.52.1
rdflib==7.6.0
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.2
boto3==1.43.64
