# Documentation only — NOT installed by any build.
# The service projects are the source of truth for Python dependencies:
#   curator-backend/pyproject.toml  (managed by uv; its requirements.txt feeds the Docker image)
# These are the core RDF/validation/test packages the project centres on.
rdflib==7.6.0
pyshacl==0.40.1
pytest==9.1.1
